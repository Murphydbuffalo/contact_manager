require "./app"
require "sinatra/activerecord/rake"
#Rake commands are Ruby scripts run from the command line
#You can use them to create, update, destroy etc. your db.
#Rake must look in the config directory's databse.yml file
#which specifies the db names for dev, testing & production.
# rake -T shows all available rake commands.